server:
  port: 8088  # 服务端口

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: gateway-service

zuul:
  routes:
    web-api:
      path: /web/**
      serviceId: web-service